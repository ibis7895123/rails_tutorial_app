<% if !current_user?(@user) %>
<!-- 自分以外のユーザーのときに表示 -->
<div id="follow_form">
  <% if current_user.following?(@user) %><!-- フォローしている -->
  <%= render "unfollow" %><!--  -->

  <% else %><!-- フォローしていない -->
  <%= render "follow" %><!-- -->

  <% end %>
</div>
<% end %>
