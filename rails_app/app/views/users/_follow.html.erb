<!-- 新規のactive_relationshipsを作成 -->
<%= form_with(model: current_user.active_relationships.build) do |form| %>
<div><%= hidden_field_tag :followed_id, @user.id %></div>

<%= form.submit "Follow", class: 'btn btn-primary' %><!--  -->
<% end %>
